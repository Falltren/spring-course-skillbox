spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/news_service_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: on
    change-log: classpath:/db/changelog/db.changelog-master.xml
